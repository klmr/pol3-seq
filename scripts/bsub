args=("$@")
job=("${args[${#args[@]}-1]}")
unset args[${#args[@]}-1]
jobname="$(basename "$(<<< "$job" sed 's/ .*$//')")"
logfile="logs/$jobname.log"
errfile="logs/$jobname.err"

bsub -J "$jobname" -o "$logfile" -e "$errfile" ${args[@]} "$job"

# vim: ft=sh
